int add (int x , int y);

//.h files are for declaration. like there exist a fun named xyz
// we can also define it here in .h but that will cause prb 
//if program gets bigger as whole purpose fo .h files is to use it in different files wihout causing any prb.

// though sometimes we doo define and declare in .h itself.

//by the way we need one .cpp file for defiantion if declaration is done by .h file to use it anywhere